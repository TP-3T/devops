@echo off
REM Check for staged .env files
git diff --cached --name-only | findstr /R "\.env$" >nul
IF %ERRORLEVEL% EQU 0 (
    echo Push blocked: .env files are staged for commit.
    exit /b 1
)
exit /b 0

Skip to content
Navigation Menu
